services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: kotan-frontend-dev
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
