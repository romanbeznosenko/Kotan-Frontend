services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.local
    container_name: kotan-frontend-local
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
